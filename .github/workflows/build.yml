name: Build and Publish

on:
  push:
    branches: [main, "release/**"]
    # App image releases are v* tags. Chart releases (epistola-*) are a separate
    # stream (helm.yml) and deliberately don't match here, so they never trigger
    # the app pipeline.
    tags: ["v*"]
  pull_request:
    branches: [main, "release/**"]
    types: [opened, synchronize, labeled]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Compile once and prime the Gradle build cache so the test jobs reuse compiled
  # outputs instead of recompiling. Every second here is on the critical path of
  # the whole pipeline (both test jobs wait for it), so this job does nothing the
  # test jobs do not need: frontend checks and SBOM/vulnerability scanning have
  # their own jobs and run alongside the tests instead of in front of them.
  compile:
    name: Compile
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      - name: Check contract version
        if: github.event_name == 'pull_request'
        run: |
          VERSION=$(grep 'epistola-contract\s*=' gradle/libs.versions.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          echo "Contract version: $VERSION"
          if [[ "$VERSION" == *-SNAPSHOT* ]] || [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+-.+ ]]; then
            echo "::warning::Contract version '$VERSION' is a snapshot/feature-stamped version. Update to a released version before merging."
          fi

      - name: Setup tools with mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      # The editor bundle is a Gradle input (modules/editor packages dist/ into its
      # jar), and `pnpm build` also runs the component-registry check.
      - name: Build frontend
        run: pnpm build

      - name: Setup Gradle cache
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6

      # One invocation, not two: the configuration cache is never reused on CI, so
      # every extra `gradle` call pays the full configuration phase again.
      # ktlintCheck and checkContractVersionAlignment ride along here because they
      # only hang off `check`, which CI stopped running when `gradle build` went
      # away — so CLAUDE.md claimed an enforcement that did not exist. Both are
      # cheap (ktlint over the whole repo is seconds) and fail fast on style or a
      # half-finished contract bump.
      - name: Check migrations, changelog, style, contract alignment and compile
        env:
          # Waives the "a change to shipped code needs a changelog fragment" requirement.
          SKIP_CHANGELOG_REQUIREMENT: ${{ contains(github.event.pull_request.labels.*.name, 'no-changelog') }}
        run: gradle checkMigrationVersions checkChangelogFragments ktlintCheck checkContractVersionAlignment testClasses

      # setup-gradle does NOT share the local build cache across jobs, so we
      # ship it explicitly. It is content-addressed (location-independent), so
      # the test jobs' compileKotlin/compileJava tasks become FROM-CACHE.
      - name: Package Gradle build cache
        run: |
          if [ -d "$HOME/.gradle/caches/build-cache-1" ]; then
            tar -C "$HOME" -czf gradle-build-cache.tgz .gradle/caches/build-cache-1
            echo "Packaged $(du -sh gradle-build-cache.tgz | cut -f1) Gradle build cache"
          else
            echo "::error::No Gradle build-cache-1 directory produced by the compile step"
            exit 1
          fi

      - name: Upload Gradle build cache
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: gradle-build-cache
          path: gradle-build-cache.tgz
          retention-days: 1
          if-no-files-found: error

  # Lint, formatting, license metadata and the editor's unit tests. Independent of
  # the backend, so it runs alongside compile rather than in front of it.
  frontend-checks:
    name: Frontend checks
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Setup tools with mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint:check

      # stylelint has a script and a config but ran nowhere, so CSS drift was
      # caught only by review.
      - name: Lint CSS
        run: pnpm lint:css

      - name: Check format
        run: pnpm format:check

      - name: Check license metadata
        run: pnpm license:check

      # The instruction files only ever grew and nothing tested them: paths went stale and guards
      # were claimed that CI never ran. This fails on that drift instead of misleading the reader.
      - name: Check agent instructions
        run: pnpm agents:check

      - name: Test frontend
        run: pnpm test

  # SBOMs, third-party notices, the VEX document and the vulnerability gate. The
  # backend SBOM is the compiled runtime classpath, so this needs compile, but the
  # test jobs need none of it: it runs alongside them instead of delaying them.
  sbom:
    name: SBOM and vulnerability scan
    runs-on: ubuntu-latest
    needs: compile
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Setup tools with mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        run: pnpm build

      - name: Setup Gradle cache
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6
        with:
          cache-read-only: true

      - name: Download Gradle build cache
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gradle-build-cache

      - name: Restore Gradle build cache (compile-once reuse)
        run: |
          mkdir -p "$HOME/.gradle/caches"
          tar -C "$HOME" -xzf gradle-build-cache.tgz
          echo "Restored Gradle build cache from the compile job"

      - name: Generate frontend third-party notices
        run: pnpm --filter @epistola/editor notices

      - name: Generate backend SBOM and third-party notices
        run: gradle :apps:epistola:generateSbom :apps:epistola:generateThirdPartyNotices

      # cdxgen runs through npx. Without this cache every run downloads it again,
      # which has taken over five minutes on slow npm days. The `sbom` script in
      # modules/editor/package.json pins the cdxgen version; the key follows it.
      - name: Cache cdxgen
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ~/.npm/_npx
          key: ${{ runner.os }}-npx-cdxgen-${{ hashFiles('modules/editor/package.json') }}

      - name: Generate frontend SBOM
        run: pnpm --filter @epistola/editor run sbom

      - name: Generate VEX document
        run: pnpm vulnerabilities:export-vex

      - name: Scan backend SBOM for vulnerabilities
        uses: aquasecurity/trivy-action@d2a0b60797ff03db6132bd4e2b293f9b37081297 # master
        env:
          TRIVY_VEX: build/vex/epistola.openvex.json
        with:
          scan-type: "sbom"
          scan-ref: "apps/epistola/build/sbom/bom.json"
          format: "table"
          exit-code: "0"
          severity: "CRITICAL"
          output: "backend-vulnerability-report.txt"

      - name: Scan frontend SBOM for vulnerabilities
        uses: aquasecurity/trivy-action@d2a0b60797ff03db6132bd4e2b293f9b37081297 # master
        env:
          TRIVY_VEX: build/vex/epistola.openvex.json
        with:
          scan-type: "sbom"
          scan-ref: "modules/editor/build/sbom.json"
          format: "table"
          exit-code: "0"
          severity: "CRITICAL"
          output: "frontend-vulnerability-report.txt"

      - name: Check vulnerability scan results
        run: |
          failed=false
          for report in backend-vulnerability-report.txt frontend-vulnerability-report.txt; do
            if [ -f "$report" ]; then
              name="${report%-vulnerability-report.txt}"
              # Check if any vulnerabilities were found (Total: N where N > 0)
              if grep -qP 'Total: [1-9]' "$report"; then
                echo "::error::Critical vulnerabilities found in $name dependencies:"
                echo ""
                cat "$report"
                echo ""
                failed=true
              fi
            fi
          done
          if [ "$failed" = true ]; then
            echo "::error::Vulnerability scan failed. Update the affected dependencies to fixed versions."
            exit 1
          fi
          echo "No critical vulnerabilities found."

      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: sbom
          path: |
            apps/epistola/build/sbom/bom.json
            modules/editor/build/sbom.json
            build/vex/epistola.openvex.json
          retention-days: 7

      - name: Upload vulnerability reports
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: always()
        with:
          name: vulnerability-reports
          path: |
            backend-vulnerability-report.txt
            frontend-vulnerability-report.txt
          retention-days: 7

  # Unit + integration via the Kover-instrumented catch-all `test` task
  # (excludes ui/perf). Runs on its own runner, in parallel with test-ui.
  test-unit-integration:
    name: Unit & Integration Tests
    runs-on: ubuntu-latest
    needs: compile
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup tools with mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        run: pnpm build

      - name: Setup Gradle cache
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6
        with:
          cache-read-only: true

      - name: Download Gradle build cache
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gradle-build-cache

      - name: Restore Gradle build cache (compile-once reuse)
        run: |
          mkdir -p "$HOME/.gradle/caches"
          tar -C "$HOME" -xzf gradle-build-cache.tgz
          echo "Restored Gradle build cache from the compile job"

      - name: Unit + integration tests (+ aggregated coverage)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gradle test koverXmlReport

      - name: Upload test reports on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: unit-integration-reports
          path: "**/build/reports/tests/"
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload aggregated coverage report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: coverage-report
          path: build/reports/kover/report.xml
          retention-days: 7
          if-no-files-found: ignore

      # Permanent test-performance evidence (per-module JSON: wall time, context
      # boots, tenant bootstraps, slowest classes, per-command dispatch profile).
      # Written by the cross-cutting harness in modules/testing; kept so test-suite
      # performance is monitorable over time and regressions are caught. always():
      # capture even when a test fails.
      - name: Upload test-run metrics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: test-run-metrics
          path: "**/build/test-metrics/*.json"
          retention-days: 30
          if-no-files-found: ignore

  # UI tests via the hardened `uiTest` task (serial, 2g, --disable-dev-shm-usage).
  # Dedicated runner = zero CPU contention (the #418 root cause). Re-run THIS job
  # alone to satisfy the #418 "3 consecutive green UI runs" gate.
  test-ui:
    name: UI Tests (Playwright)
    runs-on: ubuntu-latest
    needs: compile
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup tools with mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        run: pnpm build

      - name: Setup Gradle cache
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6
        with:
          cache-read-only: true

      - name: Download Gradle build cache
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gradle-build-cache

      - name: Restore Gradle build cache (compile-once reuse)
        run: |
          mkdir -p "$HOME/.gradle/caches"
          tar -C "$HOME" -xzf gradle-build-cache.tgz
          echo "Restored Gradle build cache from the compile job"

      # Keyed on the Playwright version alone: keying on the whole version catalog
      # re-downloaded Chromium and its apt dependencies (up to three minutes) on
      # every dependency bump.
      - name: Resolve Playwright version
        run: |
          PLAYWRIGHT_VERSION=$(grep '^playwright\s*=' gradle/libs.versions.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          echo "PLAYWRIGHT_VERSION=$PLAYWRIGHT_VERSION" >> $GITHUB_ENV

      - name: Cache Playwright browsers
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ env.PLAYWRIGHT_VERSION }}

      # Install the Chromium build + OS deps that match the pinned Playwright
      # version BEFORE the test task. Idempotent (fast no-op on a cache hit); on a
      # cache miss this stops the first UI test class from racing a browser install
      # while it also tries to launch Chromium. Java Playwright reads the same
      # ~/.cache/ms-playwright as the npm CLI of the same version.
      - name: Install Playwright browsers
        run: |
          echo "Installing Playwright $PLAYWRIGHT_VERSION (chromium, --with-deps)"
          npx --yes "playwright@${PLAYWRIGHT_VERSION}" install --with-deps chromium

      - name: UI tests
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gradle uiTest

      - name: Upload Playwright traces and test reports on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: playwright-failures
          path: |
            apps/epistola/build/playwright-traces/
            apps/epistola/build/reports/tests/
          retention-days: 7
          if-no-files-found: ignore

  # Badge only when BOTH test buckets are green (needs both) and only on a
  # push to main (unchanged behavior). The Kover .ic comes solely from the
  # instrumented `test` task, so coverage just consumes the report artifact.
  coverage:
    name: Coverage Badge
    runs-on: ubuntu-latest
    needs: [test-unit-integration, test-ui]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Download aggregated coverage report
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: coverage-report
          path: build/reports/kover

      - name: Generate coverage badge
        id: coverage
        run: |
          # Parse coverage percentage from Kover XML report
          REPORT_FILE="build/reports/kover/report.xml"
          if [ -f "$REPORT_FILE" ]; then
            # Extract instruction coverage from the summary counter (last INSTRUCTION counter in report)
            COVERED=$(grep -o 'type="INSTRUCTION"[^/]*' "$REPORT_FILE" | tail -1 | grep -oP 'covered="\K[0-9]+')
            MISSED=$(grep -o 'type="INSTRUCTION"[^/]*' "$REPORT_FILE" | tail -1 | grep -oP 'missed="\K[0-9]+')

            if [ -n "$COVERED" ] && [ -n "$MISSED" ]; then
              TOTAL=$((COVERED + MISSED))
              if [ "$TOTAL" -gt 0 ]; then
                PERCENTAGE=$((COVERED * 100 / TOTAL))
              else
                PERCENTAGE=0
              fi
            else
              PERCENTAGE=0
            fi
          else
            PERCENTAGE=0
          fi

          echo "percentage=$PERCENTAGE" >> $GITHUB_OUTPUT

          # Determine badge color based on coverage
          if [ "$PERCENTAGE" -ge 80 ]; then
            COLOR="brightgreen"
          elif [ "$PERCENTAGE" -ge 60 ]; then
            COLOR="green"
          elif [ "$PERCENTAGE" -ge 40 ]; then
            COLOR="yellow"
          elif [ "$PERCENTAGE" -ge 20 ]; then
            COLOR="orange"
          else
            COLOR="red"
          fi

          # Create shields.io endpoint JSON
          mkdir -p .github/badges
          cat > .github/badges/coverage.json << EOF
          {"schemaVersion":1,"label":"coverage","message":"${PERCENTAGE}%","color":"${COLOR}"}
          EOF

          echo "color=$COLOR" >> $GITHUB_OUTPUT

      - name: Commit coverage badge
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          FILE_PATH=".github/badges/coverage.json"

          # Get current file SHA if it exists (required for updates)
          CURRENT_SHA=$(gh api repos/${{ github.repository }}/contents/${FILE_PATH} --jq '.sha' 2>/dev/null || echo "")

          # Read new content and base64 encode it
          NEW_CONTENT=$(base64 -w0 ${FILE_PATH})

          # Check if content has changed by comparing with existing
          if [ -n "$CURRENT_SHA" ]; then
            EXISTING_CONTENT=$(gh api repos/${{ github.repository }}/contents/${FILE_PATH} --jq '.content' | tr -d '\n')
            if [ "$NEW_CONTENT" = "$EXISTING_CONTENT" ]; then
              echo "No changes to coverage badge"
              exit 0
            fi
          fi

          # Create or update the file via GitHub API (commits are automatically signed)
          if [ -n "$CURRENT_SHA" ]; then
            gh api repos/${{ github.repository }}/contents/${FILE_PATH} \
              --method PUT \
              -f message="chore: update coverage badge [skip ci]" \
              -f content="${NEW_CONTENT}" \
              -f branch="main" \
              -f sha="${CURRENT_SHA}"
          else
            gh api repos/${{ github.repository }}/contents/${FILE_PATH} \
              --method PUT \
              -f message="chore: update coverage badge [skip ci]" \
              -f content="${NEW_CONTENT}" \
              -f branch="main"
          fi

          echo "Coverage badge updated and committed (signed by GitHub)"

  docker:
    name: Build and Push ${{ matrix.variant }} Docker Image
    runs-on: ubuntu-latest
    needs: [test-unit-integration, test-ui, frontend-checks, sbom]
    # App image builds only for app releases (v* tags); chart releases
    # (epistola-*/epistola-grafana-*) must not trigger an app build.
    if: startsWith(github.ref, 'refs/tags/v') || contains(github.event.pull_request.labels.*.name, 'publish')
    concurrency:
      group: docker-publish
      cancel-in-progress: false
    permissions:
      contents: write
      packages: write
      id-token: write
    strategy:
      matrix:
        include:
          - variant: JVM
            project: ":apps:epistola"
            gradle-args: ""
            image-suffix: ""
            enabled: true
          - variant: Demo
            project: ":apps:epistola-demo"
            gradle-args: ""
            image-suffix: "-demo"
            # A separate app that depends on apps/epistola and adds demo mode. Demo mode gives every
            # user who logs in their own tenant and can carry a shared secret that authenticates
            # every /api endpoint against every tenant, so it ships as its own artifact rather than a
            # profile flag on the production image — the production image does not contain the code.
            # See docs/auth.md "Two images".
            enabled: true
          - variant: Native
            project: ":apps:epistola"
            gradle-args: -PnativeImage=true
            image-suffix: "-native"
            # Disabled: JDBI/Kotlin reflection doesn't work with GraalVM native images
            # See: https://github.com/jdbi/jdbi/issues/2475
            enabled: false
      fail-fast: false
    steps:
      - name: Check if variant is enabled
        if: ${{ !matrix.enabled }}
        run: |
          echo "::notice::${{ matrix.variant }} build is disabled"
          exit 0

      - name: Checkout
        if: ${{ matrix.enabled }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      - name: Setup tools with mise
        if: ${{ matrix.enabled }}
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4

      - name: Get pnpm store directory
        if: ${{ matrix.enabled }}
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        if: ${{ matrix.enabled }}
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install frontend dependencies
        if: ${{ matrix.enabled }}
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        if: ${{ matrix.enabled }}
        run: pnpm build

      - name: Setup Gradle cache
        if: ${{ matrix.enabled }}
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6

      - name: Log in to Container Registry
        if: ${{ matrix.enabled }}
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Cosign
        if: ${{ matrix.enabled }}
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Extract and validate version from tag
        if: ${{ matrix.enabled && startsWith(github.ref, 'refs/tags/v') }}
        id: tag
        run: |
          TAG_NAME="${GITHUB_REF_NAME}"
          VERSION="${TAG_NAME#v}"
          # The tag must match gradle.properties (blocks releasing a -SNAPSHOT).
          FILE_VER="$(grep -E '^version=' gradle.properties | head -n1 | cut -d= -f2)"
          if [ "$VERSION" != "$FILE_VER" ]; then
            echo "::error::tag ${TAG_NAME} (version ${VERSION}) != gradle.properties version ${FILE_VER}"; exit 1
          fi
          echo "new_version=${VERSION}" >> $GITHUB_OUTPUT
          echo "new_tag=${TAG_NAME}" >> $GITHUB_OUTPUT
          echo "Release version: ${VERSION}"

      - name: Get previous tag (PR)
        if: ${{ matrix.enabled && github.event_name == 'pull_request' }}
        id: previoustag
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "0.0.0")
          echo "tag=${PREV_TAG#v}" >> $GITHUB_OUTPUT

      - name: Determine build version
        if: ${{ matrix.enabled }}
        id: version
        run: |
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            VERSION="${GITHUB_REF_NAME#v}"
          elif [ "${{ github.event_name }}" = "pull_request" ]; then
            VERSION="${{ steps.previoustag.outputs.tag }}-pr${{ github.event.pull_request.number }}"
          fi
          VERSION="${VERSION:-dev}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Generate SBOMs
        if: ${{ matrix.enabled && matrix.variant == 'JVM' }}
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          gradle :apps:epistola:generateSbom -PreleaseVersion=$VERSION
          pnpm --filter @epistola/editor run sbom

      - name: Generate third-party license notices
        if: ${{ matrix.enabled && matrix.variant == 'JVM' }}
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          pnpm --filter @epistola/editor notices
          gradle :apps:epistola:generateThirdPartyNotices -PreleaseVersion=$VERSION

      - name: Build Docker image
        if: ${{ matrix.enabled }}
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: gradle ${{ matrix.project }}:bootBuildImage -PreleaseVersion=$VERSION ${{ matrix.gradle-args }} --imageName=epistola-suite:$VERSION${{ matrix.image-suffix }}

      - name: Tag and push Docker image (PR)
        if: ${{ matrix.enabled && github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'publish') }}
        id: push_pr
        env:
          VERSION: ${{ steps.version.outputs.version }}
          PR_TAG: ${{ steps.previoustag.outputs.tag }}-pr-${{ github.event.pull_request.number }}-${{ github.run_number }}
          SUFFIX: ${{ matrix.image-suffix }}
        run: |
          docker tag epistola-suite:$VERSION$SUFFIX ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:$PR_TAG$SUFFIX
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:$PR_TAG$SUFFIX
          echo "digest=$(docker inspect --format='{{index .RepoDigests 0}}' ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:$PR_TAG$SUFFIX | cut -d'@' -f2)" >> $GITHUB_OUTPUT

      - name: Sign Docker image (PR)
        if: ${{ matrix.enabled && github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'publish') }}
        env:
          DIGEST: ${{ steps.push_pr.outputs.digest }}
        run: cosign sign --yes ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${DIGEST}

      - name: Tag and push Docker image (release)
        if: ${{ matrix.enabled && startsWith(github.ref, 'refs/tags/v') }}
        id: push_release
        env:
          VERSION: ${{ steps.tag.outputs.new_version }}
          SUFFIX: ${{ matrix.image-suffix }}
        run: |
          docker tag epistola-suite:$VERSION$SUFFIX ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:$VERSION$SUFFIX
          docker tag epistola-suite:$VERSION$SUFFIX ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}$SUFFIX
          docker tag epistola-suite:$VERSION$SUFFIX ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest$SUFFIX
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:$VERSION$SUFFIX
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}$SUFFIX
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest$SUFFIX
          echo "digest=$(docker inspect --format='{{index .RepoDigests 0}}' ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:$VERSION$SUFFIX | cut -d'@' -f2)" >> $GITHUB_OUTPUT

      - name: Sign Docker image (release)
        if: ${{ matrix.enabled && startsWith(github.ref, 'refs/tags/v') }}
        env:
          DIGEST: ${{ steps.push_release.outputs.digest }}
        run: cosign sign --yes ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${DIGEST}

      - name: Attest SBOM to Docker image (release)
        if: ${{ matrix.enabled && matrix.variant == 'JVM' && startsWith(github.ref, 'refs/tags/v') }}
        env:
          DIGEST: ${{ steps.push_release.outputs.digest }}
        run: cosign attest --yes --predicate apps/epistola/build/sbom/bom.json --type cyclonedx ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${DIGEST}

      - name: Prepare release artifacts
        if: ${{ matrix.enabled && matrix.variant == 'JVM' && startsWith(github.ref, 'refs/tags/v') }}
        run: |
          mkdir -p release-artifacts
          cp apps/epistola/build/sbom/bom.json release-artifacts/epistola-backend-sbom.json
          cp modules/editor/build/sbom.json release-artifacts/epistola-editor-sbom.json
          cp apps/epistola/build/notices/THIRD-PARTY-NOTICES.md release-artifacts/THIRD-PARTY-NOTICES.md
          pnpm vulnerabilities:export-vex
          cp build/vex/epistola.openvex.json release-artifacts/epistola.openvex.json

      - name: Upload release artifacts
        if: ${{ matrix.enabled && matrix.variant == 'JVM' && startsWith(github.ref, 'refs/tags/v') }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.ref_name }}" release-artifacts/* --clobber
