---
type: fix
scopes: [ui, design-system]
audience: user
title: Styling regressions across the app are fixed.
---

Muted and small text is styled again, error messages and warning boxes read as errors and warnings rather than as plain text, and the `hidden` attribute actually hides.
